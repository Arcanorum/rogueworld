/** The Z depth of the various display containers, as set by .setDepth. */
const renderOrder = {
    ground: 1,
    statics: 2,
    dynamics: 3,
    particles: 4,
    darkness: 5,
};

export default renderOrder;
