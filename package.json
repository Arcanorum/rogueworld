{
  "name": "dungeonz",
  "version": "0.0.1",
  "scripts": {
    "postinstall": "cd server && npm install && cd ../client && npm install",
    "server": "cd server && node index",
    "client": "cd client && sass scss/Main.scss:main.css && npm start"
  },
  "dependencies": {}
}
